{
  "name": "mastersafe-native",
  "private": true,
  "version": "7.10.0",
  "description": "MasterSafe Desktop e Android com UI fiel aos mockups aprovados, IA híbrida, quota elástica e planos modulares",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build --bundles nsis",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build -- --debug --apk"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}